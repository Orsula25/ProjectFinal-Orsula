{% extends 'base.html.twig' %}

{% block titre %}Modifier une entreprise{% endblock %}

{% block contenu %}
    <h1>Modifier une entreprise</h1>

    {{ include('entreprise/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="detail-actions">
        <a href="{{ path('app_accueil') }}" class="btn-detail btn-detail-secondary">
            Retour à l'accueil
        </a>

        {{ include('entreprise/_delete_form.html.twig') }}
    </div>
{% endblock %}
