{% extends 'base.html.twig' %}

{% block titre %}Clients — Logix G-Stock{% endblock %}

{% block contenu %}

<h1>Client</h1>

<table class="table">
    <tbody>
        <tr>
            <th>id</th>
            <td>{{client.id}}</td>
            </tr>
            <tr>
            <th>Nom</th>
            <td>{{client.nom}}</td>
            </tr>
            <tr>
            <th>Prenom</th>
            <td>{{client.prenom}}</td>
            </tr>
            <tr>
            <th>Adresse</th>
            <td>{{client.adresse}}</td>
            </tr>
            <tr>
            <th>Telephone</th>
            <td>{{client.telephone}}</td>
            </tr>
            <tr>
            <th>Email</th>
            <td>{{client.email}}</td>
            </tr>


            <tr>
            <th>Actions</th>
            <td>
                <a href="{{ path('app_client_index') }}" class="btn btn-primary">Retour à la liste</a>
                <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="btn btn-warning">Modifier</a>
                {{ include('client/_delete_form.html.twig',{'client': client})}}
            </td>
            </tr>



{% endblock contenu %}

            
