{% extends 'base.html.twig' %}

{% block titre %}Fournisseurs — Logix G-Stock{% endblock %}

{% block contenu %}

<table class="table">
    <tbody>
        <tr>
            <th>id</th>
            <td>{{fournisseur.id}}</td>
            </tr>
            <tr>
            <th>Nom</th>
            <td>{{fournisseur.nom}}</td>
            </tr>
            <tr>
            <th>Adresse</th>
            <td>{{fournisseur.adresse}}</td>
            </tr>
            <tr>
            <th>Telephone</th>
            <td>{{fournisseur.telephone}}</td>
            </tr>
            <tr>
            <th>Email</th>
            <td>{{fournisseur.email}}</td>
            </tr>
            <tr>
            <th>NumTVA</th>
            <td>{{fournisseur.numTva}}</td>
            </tr>

           




    </tbody>




</table>


<div class="detail-actions">

    <a href="{{ path('app_fournisseur_index') }}" class="btn-detail btn-detail-secondary" >Afficher la liste</a>

    <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}" class="btn-detail btn-detail-secondary" >Modifier</a>

    {{ include('fournisseur/_delete_form.html.twig') }}
</div>

{% endblock contenu %}


