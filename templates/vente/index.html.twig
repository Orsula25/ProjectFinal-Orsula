{% extends 'base.html.twig' %}

{% block titre %}Ventes — Logix G-Stock{% endblock %}

{% block contenu %}
<h1 class="mb-4">🧾 Liste des ventes</h1>

<table class="table table-hover table-bordered align-middle">
  <thead class="table-dark">
    <tr>
      <th>ID</th>
      <th>Date</th>
      <th>Montant total</th>
      <th>État</th>
      <th>Créé le</th>
      <th>Modifié le</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  {% for vente in ventes %}
    <tr>
      <td>{{ vente.id }}</td>
      <td>{{ vente.dateVente ? vente.dateVente|date('d/m/Y H:i') : '' }}</td>
      <td>{{ vente.montantTotal|number_format(2, ',', ' ') }} €</td>
      <td>{{ vente.etat }}</td>
      <td>{{ vente.dateCreation ? vente.dateCreation|date('d/m/Y H:i') : '' }}</td>
      <td>{{ vente.dateModification ? vente.dateModification|date('d/m/Y H:i') : '' }}</td>
      <td>
        <a class="btn btn-sm btn-outline-primary" href="{{ path('app_vente_show', {'id': vente.id}) }}">Afficher</a>
        <a class="btn btn-sm btn-outline-warning" href="{{ path('app_vente_edit', {'id': vente.id}) }}">Modifier</a>
      </td>
    </tr>
  {% else %}
    <tr>
      <td colspan="7" class="text-center">⚠️ Aucune vente trouvée</td>
    </tr>
  {% endfor %}
  </tbody>
</table>

<a class="btn btn-success" href="{{ path('app_vente_new') }}">➕ Ajouter une vente</a>
{% endblock %}
